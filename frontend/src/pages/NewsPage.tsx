import { PageStub } from './PageStub';

export function NewsPage() {
  return (
    <PageStub
      title="Сводка новостей и чат"
      description="Skeleton-экран. Bonus contour news/chat будет реализован в Фазе 8 и изолирован от core MVP."
    />
  );
}

